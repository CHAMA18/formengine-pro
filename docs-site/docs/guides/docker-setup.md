---
slug: /guides/docker-setup
title: Docker Setup
sidebar_label: Docker Setup
description: Run FormEngine Pro with Docker Compose.
---
# Docker Setup
docker-compose up starts PostgreSQL 16 Alpine + the app. The app auto-runs migrations on startup. Persistent volume for database data. App at http://localhost:3000, PostgreSQL at localhost:5432.
