# Database
# For local Docker Compose: postgres://fep:fep_password@localhost:5432/formengine
# For Docker internal (app→db): postgres://fep:fep_password@db:5432/formengine
DATABASE_URL="postgresql://fep:fep_password@localhost:5432/formengine?schema=public"

# Supabase (placeholder values — auth flows won't work but the app will render)
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-anon-key
